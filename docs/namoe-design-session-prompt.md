This is the visual design session for Namoe Go. Product thinking is done and locked; day-one implementation is deployed. Your job is the look and feel, the design system, the asset pack, and the handoff for implementation session B. Do not reopen product scope.

Read first, in this order: `docs/namoe-design-brief.md` (what exists, the audit, what you must produce, constraints), `docs/namoe-product-brief.md` (locked decisions), `docs/working_context_brief.md` (how I work: rendered over described, one recommendation not a menu, most distinctive option, no em dashes, no exclamation marks, sentence case, Indonesian UI copy), `docs/tokens.css` (current tokens; keep variable names, add rather than rename). Look at every image in `docs/reference/`. Live app: https://namoe-proto-production.up.railway.app. I will paste the Instagram screenshots again in this chat if you need them larger.

Work in this order and stop after each step for my reaction.

1. Audit. Open the live URL in Chrome at 390px wide, screenshot Beranda, Tenant, one tenant page, Peta, Jadwal, one Sesi board, Paspor, and the organizer index (I will give you the secret URL). List everything wrong, ordered by impact per hour of work, including the embarrassing items. Compare against the design brief's audit and tell me where you disagree.
2. Direction. Render one direction as a design canvas (or HTML mockups) of the seven visitor screens at 390px and the organizer index at 1200px, using the brand from the reference posters: cream ground, saturated blocks, navy text, chunky rounded type, blob mascots as art, one texture per screen. Build the tenant placeholder system and the passport (empty, 3 of 5, complete with redeem code) with the most care; those are the two screens that decide whether this looks finished. If you show alternatives, show two, one line each on the tradeoff, and say which you would pick.
3. System and assets. After I approve the direction: final `tokens.css`, a component sheet, and the asset pack as SVG code (tab icons, UI icons, ten category glyphs, five mascots, stamp badge states, empty-state art, favicon, placeholder generator rules). The exact inventory is in the design brief. Render the asset sheet so I can see it in one view.
4. Handoff. Write `docs/namoe-design-spec.md` (tokens diff, component-by-component changes and removals, asset paths under `docs/assets/`, acceptance checklist) and `docs/namoe-claude-code-prompt-b.md` (paste-ready prompt for implementation session B: apply the design first, then P1, then P2 if time allows; same process rules as `docs/namoe-claude-code-prompt.md`: code only, no git, no Railway, checkpoints, tell me when clean). Save the SVG assets as files under `docs/assets/`.

Hard rules: no image files, no icon libraries, no new dependencies; everything is inline SVG or CSS. Colours only in tokens. Do not touch floor map geometry. No clock or live states. Tap targets 44px, body text 16px. Mascots never sit behind text. Do not redraw the Namoe Market wordmark; define the Namoe Go text lockup and note that Theo supplies the real logo.

Start with step 1.
